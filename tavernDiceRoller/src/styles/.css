/* App.css — imported in App.jsx
   Relies on tokens defined in index.css */

/* ─── Sheet Root ──────────────────────────────────── */
.charSheet {
  font-family: var(--ff-body);
  font-size: 15px;
  color: var(--ink);
  background: var(--parchment);
  min-height: 100vh;
  padding: 28px 22px 80px;
  border: 1px solid var(--gold);
  border-radius: 3px;
  overflow: hidden;
}

/* ─── Responsive Sheet Layout ─────────────────────── */
.charSheet-scroll {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.charSheet-inner {
  min-width: 780px;
}

.charSheet-columns {
  display: grid;
  grid-template-columns: 240px 1fr 260px;
  gap: 14px;
  align-items: start;
}

.charSheet-col { min-width: 0; }

/* ─── Layout Containment ──────────────────────────── */
.charSheet .row {
  --bs-gutter-x: 1rem;
}

/* ─── Typography ──────────────────────────────────── */
.charSheet h1,
.charSheet h2,
.charSheet h3,
.charSheet h5,
.charSheet h6 {
  font-family: var(--ff-display);
  color: var(--crimson);
  letter-spacing: 0.04em;
}

/* h4 only outside combatStats */
.charSheet h4 {
  font-family: var(--ff-display);
  color: var(--crimson);
  font-size: 0.82rem;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  margin-bottom: 6px;
}

.charSheet label {
  font-family: var(--ff-body);
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--ink-muted);
  letter-spacing: 0.02em;
}

.charSheet p {
  color: var(--ink);
  font-size: 1rem;
  margin: 0;
}

.charSheet hr::after { background: var(--parchment); }

/* ─── Header Block ────────────────────────────────── */
.headerBlock {
  border: 1px solid var(--border);
  border-radius: 3px;
  padding: 14px;
  margin-bottom: 14px;
}

.headerBlock label {
  font-family: var(--ff-display);
  font-size: 0.68rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--gold);
  display: flex;
  align-items: center;
  gap: 6px;
}

.namePlate {
  font-family: var(--ff-display) !important;
  font-size: 1.4rem !important;
  font-weight: 600;
  color: var(--crimson) !important;
  letter-spacing: 0.04em;
  text-align: center;
  background: transparent;
  border: none !important;
  border-bottom: 1px solid var(--gold) !important;
  width: 100%;
  padding: 3px 8px;
  box-shadow: none !important;
}

.classAndLevel,
.race,
.background,
.experience {
  background: transparent;
  border: none;
  border-bottom: 1px solid var(--border);
  font-family: var(--ff-body);
  font-size: 0.92rem;
  color: var(--ink);
  width: 100%;
  padding: 2px 4px;
}

/* ─── Section Boxes ───────────────────────────────── */
.skills,
.proficiencies,
.combatStats,
.attacks,
.equipment,
.currency,
.personality {
  border: 1px solid var(--border);
  border-radius: 3px;
  padding: 12px 10px;
  position: relative;
  margin-bottom: 14px;
}

.skills::before        { content: 'ABILITIES & SKILLS'; }
.proficiencies::before { content: 'PROFICIENCIES'; }
.combatStats::before   { content: 'COMBAT'; }
.currency::before      { content: 'COIN'; }
.personality::before   { content: 'CHARACTER'; }
.attacks::before       { content: 'ATTACKS & SPELLCASTING'; }
.equipment::before     { content: 'EQUIPMENT'; }

.skills::before,
.proficiencies::before,
.combatStats::before,
.currency::before,
.personality::before,
.attacks::before,
.equipment::before {
  position: absolute;
  top: -8px;
  left: 12px;
  background: var(--parchment);
  padding: 0 6px;
  font-family: var(--ff-display);
  font-size: 0.58rem;
  letter-spacing: 0.1em;
  color: var(--gold);
}

/* ─── Stat / Skill Blocks ─────────────────────────── */
.skills > div {
  border: 1px solid var(--border);
  border-radius: 2px;
  padding: 6px 8px;
  margin-bottom: 8px;
  overflow: hidden;
}
.skills > div:last-child { margin-bottom: 0; }

.skills > div > div:last-child {
  min-width: 0;
  flex: 1;
}

.skills > div > div:first-child > p:first-child {
  font-family: var(--ff-display);
  font-size: 0.65rem;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  color: var(--ink-muted);
  font-weight: 600;
  margin-bottom: 2px;
}

.skills > div > div:first-child > p:nth-child(2) {
  font-family: var(--ff-display);
  font-size: 1.4rem;
  font-weight: 600;
  color: var(--ink);
  line-height: 1;
  margin-bottom: 2px;
}

.skills > div > div:first-child > p:nth-child(3) {
  font-size: 0.8rem;
  color: var(--crimson);
}

.skills label {
  font-size: 0.78rem;
  text-transform: none;
  color: var(--ink);
  display: flex;
  align-items: center;
  gap: 4px;
  margin-bottom: 2px;
  flex-wrap: wrap;
  word-break: break-word;
}

.skills label span {
  font-family: var(--ff-display);
  font-size: 0.68rem;
  font-weight: 600;
  color: var(--crimson);
  min-width: 20px;
  text-align: right;
}

.skills input[type="checkbox"] {
  accent-color: var(--crimson);
  width: 11px;
  height: 11px;
  cursor: pointer;
}

/* ─── Combat Stats ────────────────────────────────── */

/* keep inner bootstrap cols side by side, never stack */
.combatStats > .row {
  display: flex !important;
  flex-wrap: nowrap !important;
}

.combatStats .col-4,
.combatStats .col-5 {
  flex: 0 0 auto !important;
  width: auto !important;
}

/* compact inputs — don't stretch */
.combatStats input[type="text"] {
  width: 70px !important;
  display: block;
  margin-top: 2px;
  margin-bottom: 6px;
}

/* death saving throws — plain label, not a heading */
.combatStats h4 {
  font-family: var(--ff-body) !important;
  font-size: 0.75rem !important;
  color: var(--ink-muted) !important;
  text-transform: uppercase !important;
  letter-spacing: 0.05em !important;
  font-weight: 600 !important;
  margin-bottom: 4px !important;
}

/* ─── Tables ──────────────────────────────────────── */
.charSheet table {
  border-collapse: collapse;
  table-layout: fixed;
  width: 100%;
}

.charSheet th {
  font-family: var(--ff-display);
  font-size: 0.62rem;
  letter-spacing: 0.07em;
  color: var(--ink-muted);
  border-bottom: 1px solid var(--border);
  padding: 4px 6px;
  text-align: left;
}

.charSheet td {
  padding: 4px 6px;
  border-bottom: 1px dotted var(--border);
}

/* ─── Form Elements ───────────────────────────────── */
.charSheet input[type="text"],
.charSheet input[type="number"],
.charSheet textarea {
  background: rgba(255, 255, 255, 0.25);
  border: 1px solid var(--border);
  border-radius: 2px;
  font-family: var(--ff-body);
  font-size: 0.9rem;
  color: var(--ink);
  padding: 3px 6px;
  transition: border-color 0.2s;
}

.charSheet input[type="text"]:focus,
.charSheet input[type="number"]:focus,
.charSheet textarea:focus {
  outline: none;
  border-color: var(--gold);
}

.charSheet textarea {
  width: 100%;
  max-width: 100%;
  resize: vertical;
}

.charSheet td input[type="text"],
.charSheet td textarea {
  width: 100%;
  max-width: 100%;
  min-width: 0;
}

/* ─── Buttons ─────────────────────────────────────── */
.charSheet button {
  font-family: var(--ff-display);
  font-size: 0.68rem;
  letter-spacing: 0.07em;
  text-transform: uppercase;
  color: var(--parchment);
  background: var(--crimson);
  border: 1px solid var(--crimson-dark);
  border-radius: 2px;
  padding: 5px 14px;
  cursor: pointer;
  transition: background 0.2s;
  margin-top: 6px;
}
.charSheet button:hover  { background: var(--crimson-glow); }
.charSheet button:active { background: var(--crimson-dark); }